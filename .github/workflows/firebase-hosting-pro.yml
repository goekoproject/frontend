# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy Pro
on:
  push:
    branches:
      - main
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./goeko
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Install Node 18
        uses: actions/setup-node@v4
        with:
          node-version: 18
          check-latest: true
      #Init Node
      - name: Install Dependencies
        run: npm install
      - name: Build
        run: npm run build goeko --production
      - name: Archive Production Artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-project
          path: goeko/dist/apps
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: build-project
          path: goeko/dist/apps
      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting:1//03yutOTKP0ul_CgYIARAAGAMSNwF-L9IrdEyrGm5WnxXRT1S2VfW3lA2RbZ4RGIOlcGd1WfOZ53LpGwXJRlUCzVR8bzM9DEL6EDY -P goeko-pro
        env:
          FIREBASE_TOKEN: "${{ secrets.FIREBASE_TOKEN }}"
          PROJECT_PATH : ./goeko
