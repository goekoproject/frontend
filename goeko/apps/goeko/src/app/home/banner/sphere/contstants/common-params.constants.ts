import { MeshActorsPosition } from '../models/sphere.model';

export const COMMON_PARAMS = {
	DIAMETER_WRAPPER: 6,
	DIAMETER_INNER: 4,
	DISTANCE: 4,
};

export const POSITION_DIRECTIONAL_LIGHT = new MeshActorsPosition(2, -3.2, 0);
