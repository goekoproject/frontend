export interface ClassificationCodeRequest {
  mainCategory: string
  subCategory: string
  products: string[]
}
export interface ContactRequest {
  name: string
  email: string
  phoneNumber: string
}
